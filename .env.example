# ── Agent ─────────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model used (default: claude-opus-4-6)
# ANTHROPIC_MODEL=claude-opus-4-6

# Optional: max tool call turns per session (default: 20)
# MAX_TURNS=20

# ── Tavily (web search) ───────────────────────────────────────────────────────
# Free tier: 1000 searches/month — https://tavily.com
TAVILY_API_KEY=tvly-...

# ── GitHub Pages blog ─────────────────────────────────────────────────────────
# Token needs: repo + pages scopes
# https://github.com/settings/tokens/new
GITHUB_TOKEN=ghp_...

# Format: username/repo-name  (e.g. alice/free-agent-blog)
GITHUB_BLOG_REPO=username/free-agent-blog

# Your GitHub Pages URL (e.g. https://username.github.io/free-agent-blog)
GITHUB_PAGES_URL=https://username.github.io/free-agent-blog

# ── Twitter / X ───────────────────────────────────────────────────────────────
# Free tier supports 500 tweets/month — https://developer.twitter.com
# Leave blank to disable Twitter posting
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=

# ── Bluesky ───────────────────────────────────────────────────────────────────
# Create an App Password at: https://bsky.app/settings/app-passwords
# Leave blank to disable Bluesky posting
BLUESKY_HANDLE=yourhandle.bsky.social
BLUESKY_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# ── AgentMail (agent's email inbox) ──────────────────────────────────────────
# Get a free API key at https://agentmail.to
# Run setup_email.py once to create the inbox, then paste the inbox ID here.
# Leave blank to disable email features.
AGENTMAIL_API_KEY=
AGENTMAIL_INBOX_ID=    # e.g. inbox_abc123

# Optional: custom username for the inbox (default: free-agent)
# AGENTMAIL_USERNAME=free-agent

# ── Optional ──────────────────────────────────────────────────────────────────
# Timezone for display purposes (doesn't affect cron — configure that separately)
AGENT_TIMEZONE=America/New_York

# Path to SQLite database (default: /app/data/agent.db inside Docker)
# DB_PATH=/app/data/agent.db
